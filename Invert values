public class Kata {
        public static int[] invert(int[] array) {
        int[] resultEmpty = {};
            if ((array == null) || (array.length == 0)) return resultEmpty;
            for (int i=0; i < array.length; i++ ) array[i] = -array[i];
        return array;
        }
}
